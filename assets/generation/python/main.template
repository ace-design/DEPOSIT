import serial
import re
import threading
import time
import datetime

BOARD_ID = #@board_id@#
OFFSET = #@period@#

#@update@#
#@flush@#

def program():
	global BOARD_ID
	global OFFSET
#@use_global_variables@#
#@datacollectionpolicy@#
	flush()

def currentTime():
	datetime.datetime.now().time()

def send(v):
	send(v, v["src"]);

def send(v, origin):
	v["src"] = origin
	print(v)